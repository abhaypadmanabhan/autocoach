# Test-only deps (not needed at runtime). Pinned to the versions the suite
# is known-green against. Installed by CI on top of requirements.txt.
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1
