{"question": "What is the Transformer architecture based on, and what does it dispense with?", "source_chunk_text": "The dominant sequence transduction models are based on complex recurrent or convolutional neural networks that include an encoder and a decoder. The best performing models also connect the encoder and decoder through an attention mechanism. We propose a new simple network architecture, the Transformer, based solely on attention mechanisms, dispensing with recurrence and convolutions entirely.", "ideal_answer": "The Transformer is a network architecture based solely on attention mechanisms, dispensing entirely with the recurrence and convolutions used by dominant sequence transduction models.", "concept_label": "transformer architecture"}
{"question": "What BLEU scores did the model achieve on the WMT 2014 English-to-German and English-to-French translation tasks?", "source_chunk_text": "Our model achieves 28.4 BLEU on the WMT 2014 English- to-German translation task, improving over the existing best results, including ensembles, by over 2 BLEU. On the WMT 2014 English-to-French translation task, our model establishes a new single-model state-of-the-art BLEU score of 41.0 after training for 3.5 days on eight GPUs, a small fraction of the training costs of the best models from the literature.", "ideal_answer": "It achieved 28.4 BLEU on WMT 2014 English-to-German, improving over the existing best results including ensembles by over 2 BLEU, and a new single-model state-of-the-art 41.0 BLEU on English-to-French after training for 3.5 days on eight GPUs.", "concept_label": "translation results"}
{"question": "Why does the sequential nature of recurrent models limit training efficiency?", "source_chunk_text": "Recurrent models typically factor computation along the symbol positions of the input and output sequences. Aligning the positions to steps in computation time, they generate a sequence of hidden statesht, as a function of the previous hidden stateht1and the input for positiont. This inherently sequential nature precludes parallelization within training examples, which becomes critical at longer sequence lengths, as memory constraints limit batching across examples.", "ideal_answer": "Recurrent models generate hidden states one position at a time, each depending on the previous hidden state and the current input, which precludes parallelization within training examples; this becomes critical at longer sequence lengths because memory constraints limit batching across examples.", "concept_label": "recurrent model limitations"}
{"question": "What do attention mechanisms allow sequence models to do, and how were they typically used?", "source_chunk_text": "Attention mechanisms have become an integral part of compelling sequence modeling and transduc- tion models in various tasks, allowing modeling of dependencies without regard to their distance in the input or output sequences [2,16]. In all but a few cases [22], however, such attention mechanisms are used in conjunction with a recurrent network.", "ideal_answer": "Attention mechanisms allow modeling of dependencies without regard to their distance in the input or output sequences, but in all but a few cases they were used in conjunction with a recurrent network.", "concept_label": "attention mechanisms"}
{"question": "In ConvS2S and ByteNet, how does the number of operations needed to relate signals from two arbitrary positions grow with distance?", "source_chunk_text": "The goal of reducing sequential computation also forms the foundation of the Extended Neural GPU [20], ByteNet [15] and ConvS2S [8], all of which use convolutional neural networks as basic building block, computing hidden representations in parallel for all input and output positions. In these models, the number of operations required to relate signals from two arbitrary input or output positions grows in the distance between positions, linearly for ConvS2S and logarithmically for ByteNet.", "ideal_answer": "In these convolutional models the number of operations grows with the distance between positions: linearly for ConvS2S and logarithmically for ByteNet.", "concept_label": "convolutional sequence models"}
{"question": "What is self-attention and in which tasks has it been used successfully?", "source_chunk_text": "Self-attention, sometimes called intra-attention is an attention mechanism relating different positions of a single sequence in order to compute a representation of the sequence. Self-attention has been used successfully in a variety of tasks including reading comprehension, abstractive summarization, textual entailment and learning task-independent sentence representations [4, 22, 23, 19].", "ideal_answer": "Self-attention, sometimes called intra-attention, is an attention mechanism that relates different positions of a single sequence to compute a representation of that sequence; it has been used successfully in reading comprehension, abstractive summarization, textual entailment, and learning task-independent sentence representations.", "concept_label": "self-attention"}
{"question": "What makes the Transformer the first of its kind among transduction models?", "source_chunk_text": "To the best of our knowledge, however, the Transformer is the rst transduction model relying entirely on self-attention to compute representations of its input and output without using sequence- aligned RNNs or convolution.", "ideal_answer": "The Transformer is the first transduction model that relies entirely on self-attention to compute representations of its input and output, without using sequence-aligned RNNs or convolution.", "concept_label": "transformer novelty"}
{"question": "In what sense is the model auto-regressive when generating the output sequence?", "source_chunk_text": "Givenz, the decoder then generates an output sequence(y1,. . . ,ym)of symbols one element at a time. At each step the model is auto-regressive [9], consuming the previously generated symbols as additional input when generating the next.", "ideal_answer": "The decoder generates the output sequence one symbol at a time, and at each step the model consumes the previously generated symbols as additional input when generating the next one.", "concept_label": "auto-regressive decoding"}
{"question": "What is applied around each sub-layer, and what output dimension do the sub-layers and embedding layers produce?", "source_chunk_text": "We employ a residual connection [10] around each of the two sub-layers, followed by layer normalization [1]. That is, the output of each sub-layer is LayerNorm(x+S u b l a y e r (x)), whereSublayer(x)is the function implemented by the sub-layer itself. To facilitate these residual connections, all sub-layers in the model, as well as the embedding layers, produce outputs of dimensiondmodel= 512.", "ideal_answer": "A residual connection is employed around each of the two sub-layers, followed by layer normalization, so each sub-layer outputs LayerNorm(x + Sublayer(x)); to facilitate these residual connections, all sub-layers and the embedding layers produce outputs of dimension 512.", "concept_label": "residual connections"}
{"question": "How does an attention function map its inputs to an output?", "source_chunk_text": "An attention function can be described as mapping a query and a set of key-value pairs to an output, where the query, keys, values, and output are all vectors. The output is computed as a weighted sum of the values, where the weight assigned to each value is computed by a compatibility function of the query with the corresponding key.", "ideal_answer": "An attention function maps a query and a set of key-value pairs, all vectors, to an output computed as a weighted sum of the values, where each value's weight is computed by a compatibility function of the query with the corresponding key.", "concept_label": "attention function"}
{"question": "How are queries, keys, and values organized when attention is computed in practice?", "source_chunk_text": "In practice, we compute the attention function on a set of queries simultaneously, packed together into a matrixQ. The keys and values are also packed together into matricesKandV.", "ideal_answer": "The attention function is computed on a set of queries simultaneously, with the queries packed together into a matrix Q and the keys and values packed together into matrices K and V.", "concept_label": "scaled dot-product attention"}
{"question": "Why is dot-product attention preferred over additive attention despite similar theoretical complexity?", "source_chunk_text": "While the two are similar in theoretical complexity, dot-product attention is much faster and more space-efcient in practice, since it can be implemented using highly optimized matrix multiplication code. While for small values ofdkthe two mechanisms perform similarly, additive attention outperforms dot product attention without scaling for larger values ofdk[3].", "ideal_answer": "Dot-product attention is much faster and more space-efficient in practice because it can be implemented with highly optimized matrix multiplication code, although without scaling additive attention outperforms it for larger key dimensions.", "concept_label": "dot-product vs additive attention"}
{"question": "What advantage does multi-head attention give the model over a single attention head?", "source_chunk_text": "On each of these projected versions of queries, keys and values we then perform the attention function in parallel, yieldingdv-dimensional output values. These are concatenated and once again projected, resulting in the nal values, as depicted in Figure 2. Multi-head attention allows the model to jointly attend to information from different representation subspaces at different positions. With a single attention head, averaging inhibits this.", "ideal_answer": "Multi-head attention lets the model jointly attend to information from different representation subspaces at different positions, whereas with a single attention head averaging inhibits this; the parallel heads' outputs are concatenated and projected again to produce the final values.", "concept_label": "multi-head attention"}
{"question": "How many attention heads are used, and why does using multiple heads not increase the total computational cost?", "source_chunk_text": "In this work we employh=8parallel attention layers, or heads. For each of these we use dk=dv=dmodel/h= 64. Due to the reduced dimension of each head, the total computational cost is similar to that of single-head attention with full dimensionality.", "ideal_answer": "Eight parallel attention heads are used, each with key and value dimensions of 64; because each head's dimension is reduced, the total computational cost is similar to that of single-head attention with full dimensionality.", "concept_label": "attention heads"}
{"question": "In the encoder's self-attention layers, where do the keys, values, and queries come from?", "source_chunk_text": "The encoder contains self-attention layers. In a self-attention layer all of the keys, values and queries come from the same place, in this case, the output of the previous layer in the encoder. Each position in the encoder can attend to all positions in the previous layer of the encoder.", "ideal_answer": "In the encoder's self-attention layers, all of the keys, values, and queries come from the same place: the output of the previous encoder layer, and each position can attend to all positions in that previous layer.", "concept_label": "encoder self-attention"}
{"question": "What does the position-wise feed-forward network in each encoder and decoder layer consist of?", "source_chunk_text": "In addition to attention sub-layers, each of the layers in our encoder and decoder contains a fully connected feed-forward network, which is applied to each position separately and identically. This consists of two linear transformations with a ReLU activation in between.", "ideal_answer": "Each encoder and decoder layer contains a fully connected feed-forward network applied to each position separately and identically, consisting of two linear transformations with a ReLU activation in between.", "concept_label": "feed-forward network"}
{"question": "How are input and output tokens converted to vectors, and what weight sharing is used with the pre-softmax transformation?", "source_chunk_text": "Similarly to other sequence transduction models, we use learned embeddings to convert the input tokens and output tokens to vectors of dimensiondmodel. We also use the usual learned linear transfor- mation and softmax function to convert the decoder output to predicted next-token probabilities. In our model, we share the same weight matrix between the two embedding layers and the pre-softmax linear transformation, similar to [24].", "ideal_answer": "Learned embeddings convert input and output tokens to vectors of dimension dmodel, a learned linear transformation and softmax convert the decoder output to next-token probabilities, and the same weight matrix is shared between the two embedding layers and the pre-softmax linear transformation.", "concept_label": "embeddings and weight sharing"}
{"question": "Why was the sinusoidal positional encoding chosen over learned positional embeddings?", "source_chunk_text": "We also experimented with using learned positional embeddings [8] instead, and found that the two versions produced nearly identical results (see Table 3 row (E)). We chose the sinusoidal version because it may allow the model to extrapolate to sequence lengths longer than the ones encountered during training.", "ideal_answer": "Learned positional embeddings produced nearly identical results, but the sinusoidal version was chosen because it may allow the model to extrapolate to sequence lengths longer than those encountered during training.", "concept_label": "positional encoding"}
{"question": "What three desiderata motivate the use of self-attention layers?", "source_chunk_text": "Motivating our use of self-attention we consider three desiderata. One is the total computational complexity per layer. Another is the amount of computation that can be parallelized, as measured by the minimum number of sequential operations required. The third is the path length between long-range dependencies in the network.", "ideal_answer": "The three desiderata are the total computational complexity per layer, the amount of computation that can be parallelized as measured by the minimum number of sequential operations required, and the path length between long-range dependencies in the network.", "concept_label": "why self-attention"}
{"question": "How do self-attention layers and recurrent layers compare in the number of sequential operations needed to connect all positions?", "source_chunk_text": "The shorter these paths between any combination of positions in the input and output sequences, the easier it is to learn long-range dependencies [11]. Hence we also compare the maximum path length between any two input and output positions in networks composed of the different layer types. As noted in Table 1, a self-attention layer connects all positions with a constant number of sequentially executed operations, whereas a recurrent layer requiresO(n)sequential operations.", "ideal_answer": "A self-attention layer connects all positions with a constant number of sequentially executed operations, whereas a recurrent layer requires O(n) sequential operations; shorter paths between positions make it easier to learn long-range dependencies.", "concept_label": "path length"}
{"question": "How could self-attention be adapted for tasks with very long sequences, and what is the trade-off?", "source_chunk_text": "To improve computational performance for tasks involving very long sequences, self-attention could be restricted to considering only a neighborhood of sizerin 6 the input sequence centered around the respective output position. This would increase the maximum path length toO(n/r).", "ideal_answer": "Self-attention could be restricted to considering only a neighborhood of size r in the input sequence centered around each output position, which improves computational performance but increases the maximum path length to O(n/r).", "concept_label": "restricted self-attention"}
{"question": "What interpretability benefit can self-attention provide?", "source_chunk_text": "As side benet, self-attention could yield more interpretable models. We inspect attention distributions from our models and present and discuss examples in the appendix. Not only do individual attention heads clearly learn to perform different tasks, many appear to exhibit behavior related to the syntactic and semantic structure of the sentences.", "ideal_answer": "Self-attention can yield more interpretable models: inspecting attention distributions shows that individual attention heads clearly learn to perform different tasks, and many appear to exhibit behavior related to the syntactic and semantic structure of sentences.", "concept_label": "interpretability"}
{"question": "What training datasets and vocabularies were used for the translation experiments?", "source_chunk_text": "We trained on the standard WMT 2014 English-German dataset consisting of about 4.5 million sentence pairs. Sentences were encoded using byte-pair encoding [3], which has a shared source- target vocabulary of about 37000 tokens. For English-French, we used the signicantly larger WMT 2014 English-French dataset consisting of 36M sentences and split tokens into a 32000 word-piece vocabulary [31].", "ideal_answer": "Training used the standard WMT 2014 English-German dataset of about 4.5 million sentence pairs, encoded with byte-pair encoding using a shared source-target vocabulary of about 37000 tokens, and the larger WMT 2014 English-French dataset of 36M sentences with a 32000 word-piece vocabulary.", "concept_label": "training data"}
{"question": "What hardware was used for training, and how long did the base and big models train?", "source_chunk_text": "We trained our models on one machine with 8 NVIDIA P100 GPUs. For our base models using the hyperparameters described throughout the paper, each training step took about 0.4 seconds. We trained the base models for a total of 100,000 steps or 12 hours. For our big models,(described on the bottom line of table 3), step time was 1.0 seconds. The big models were trained for 300,000 steps (3.5 days).", "ideal_answer": "Training used one machine with 8 NVIDIA P100 GPUs; base models trained for 100,000 steps or 12 hours at about 0.4 seconds per step, and big models trained for 300,000 steps (3.5 days) at 1.0 seconds per step.", "concept_label": "training hardware"}
{"question": "Where is residual dropout applied during training, and at what rate for the base model?", "source_chunk_text": "Residual DropoutWe apply dropout [27] to the output of each sub-layer, before it is added to the sub-layer input and normalized. In addition, we apply dropout to the sums of the embeddings and the positional encodings in both the encoder and decoder stacks. For the base model, we use a rate of Pdrop=0.1.", "ideal_answer": "Dropout is applied to the output of each sub-layer before it is added to the sub-layer input and normalized, and also to the sums of the embeddings and the positional encodings in both encoder and decoder stacks, with a rate of 0.1 for the base model.", "concept_label": "residual dropout"}
{"question": "What effect does label smoothing have on perplexity and translation quality?", "source_chunk_text": "Label SmoothingDuring training, we employed label smoothing of valuels=0.1[30]. This hurts perplexity, as the model learns to be more unsure, but improves accuracy and BLEU score.", "ideal_answer": "Label smoothing of value 0.1 hurts perplexity because the model learns to be more unsure, but it improves accuracy and BLEU score.", "concept_label": "label smoothing"}
{"question": "How did the big model perform on English-to-French translation, and what dropout rate did it use?", "source_chunk_text": "On the WMT 2014 English-to-French translation task, our big model achieves a BLEU score of41.0, outperforming all of the previously published single models, at less than1/4the training cost of the previous state-of-the-art model. The Transformer (big) model trained for English-to-French used dropout ratePdrop=0.1, instead of0.3.", "ideal_answer": "On WMT 2014 English-to-French the big model achieved a BLEU score of 41.0, outperforming all previously published single models at less than 1/4 the training cost of the previous state of the art, and it used a dropout rate of 0.1 instead of 0.3.", "concept_label": "english-to-french results"}
{"question": "What happens to translation quality as the number of attention heads is varied while keeping computation constant?", "source_chunk_text": "In Table 3 rows (A), we vary the number of attention heads and the attention key and value dimensions, keeping the amount of computation constant, as described in Section 3.2.2. While single-head attention is 0.9 BLEU worse than the best setting, quality also drops off with too many heads.", "ideal_answer": "With computation held constant, single-head attention is 0.9 BLEU worse than the best setting, but quality also drops off with too many heads.", "concept_label": "head count ablation"}
{"question": "What does the Transformer replace in traditional encoder-decoder architectures?", "source_chunk_text": "In this work, we presented the Transformer, the rst sequence transduction model based entirely on attention, replacing the recurrent layers most commonly used in encoder-decoder architectures with multi-headed self-attention.", "ideal_answer": "The Transformer replaces the recurrent layers most commonly used in encoder-decoder architectures with multi-headed self-attention, making it the first sequence transduction model based entirely on attention.", "concept_label": "transformer summary"}
{"question": "What future applications and extensions are planned for attention-based models?", "source_chunk_text": "We are excited about the future of attention-based models and plan to apply them to other tasks. We plan to extend the Transformer to problems involving input and output modalities other than text and to investigate local, restricted attention mechanisms to efciently handle large inputs and outputs such as images, audio and video.", "ideal_answer": "Plans include applying attention-based models to other tasks, extending the Transformer to input and output modalities other than text, and investigating local, restricted attention mechanisms to efficiently handle large inputs and outputs such as images, audio, and video.", "concept_label": "future work"}
